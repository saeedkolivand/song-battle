---
description: Audit and clean dead code (unused files/exports/deps) across the monorepo
argument-hint: [package-or-path]
---

Use the cleanup subagent to audit dead code in $ARGUMENTS (whole repo if empty). Report findings by tier and stop for confirmation before deleting anything.
