---
description: Audit code quality (clean-code/DRY/KISS) and report by severity — no edits
argument-hint: [package-or-path]
---

Load the `token-efficiency` + `code-quality` skills. Use the code-quality-reviewer subagent to audit $ARGUMENTS (current diff if empty). Report violations by severity and stop — do not edit.
