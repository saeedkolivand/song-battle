// Raw hex of the overlay theme colours — mirrors the `@theme` tokens in index.css.
// Needed where a CSS class won't do (canvas-confetti particles, inline glow shadows).
export const COLOR_A = '#34d399'; // emerald (side A)
export const COLOR_B = '#38bdf8'; // sky (side B)
export const COLOR_ACCENT = '#fbbf24'; // amber (winner / ambient accent)
